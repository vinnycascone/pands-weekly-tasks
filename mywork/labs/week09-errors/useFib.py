# a program that imports myFuntions to prompt a user for a number and return the fibonacci sequence

import myFunctions as mf

number=int(input('please input a integer number:'))
print(mf.fibonacci(number))
